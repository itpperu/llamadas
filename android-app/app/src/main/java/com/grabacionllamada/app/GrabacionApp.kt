package com.grabacionllamada.app

import android.app.Application

class GrabacionApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Espacio para inicializaciones generales (Ej: Inyección manual si es necesario)
    }
}
